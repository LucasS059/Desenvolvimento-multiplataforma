// importar o modulo para entrada e saida
// importar o modulo para gerar valores aleatorios
// criar um enum que representa as opções (pedra, papel ...)

enum opcoes {Pedra, Papel, Tesoura}
void main() {
  // loop do jogo, excuta até o usuario decidir sair
  

    // exbir menu
    // capturar a opção do usuario e validar com do/while (só vale 1 a 4)

    // sortear a opção do computador

    // mapear a opção do usuario, de int para enum

    // mapear a opção do computador, de int para enum

    // exbir as opções de cada um 
    // JOGADOR(pedra) vs (tesouro)COMPUTADOR

    // Verificar e exibir o resultado /ganho do jogador, ganho do computador ou empate
}
